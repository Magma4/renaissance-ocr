# `src/data`

This folder is reserved for small code-side data assets such as:

- schema notes
- prompt fragments
- future label maps or lexicons

The current baseline keeps runtime inputs and outputs under the top-level `data/` directory instead.
